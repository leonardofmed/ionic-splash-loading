import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-splash-loading',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
